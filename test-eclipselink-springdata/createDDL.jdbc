CREATE TABLE SIMPLE (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE employees (employee_id INTEGER NOT NULL, ADDRESS VARCHAR(255), EMAIL VARCHAR(255), first_name VARCHAR(255), last_name VARCHAR(255), VERSION INTEGER, department_id INTEGER, manager_id INTEGER, PRIMARY KEY (employee_id))
CREATE TABLE HOTEL (ID BIGINT NOT NULL, ADDRESS VARCHAR(255) NOT NULL, NAME VARCHAR(255) NOT NULL, ZIP VARCHAR(255) NOT NULL, CITY_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE REVIEW (ID BIGINT NOT NULL, CHECKINDATE DATE NOT NULL, DETAILS VARCHAR(5000) NOT NULL, idx INTEGER NOT NULL, RATING INTEGER NOT NULL, TITLE VARCHAR(255) NOT NULL, TRIPTYPE INTEGER NOT NULL, HOTEL_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE CITY (ID  SERIAL NOT NULL, COUNTRY VARCHAR(255) NOT NULL, MAP VARCHAR(255) NOT NULL, NAME VARCHAR(255) NOT NULL, STATE VARCHAR(255) NOT NULL, PRIMARY KEY (ID))
CREATE TABLE departments (department_id INTEGER NOT NULL, department_name VARCHAR(255), VERSION INTEGER, manager_id INTEGER, PRIMARY KEY (department_id))
CREATE TABLE departments_employees (Department_department_id INTEGER NOT NULL, employees_employee_id INTEGER NOT NULL, PRIMARY KEY (Department_department_id, employees_employee_id))
ALTER TABLE employees ADD CONSTRAINT FK_employees_department_id FOREIGN KEY (department_id) REFERENCES departments (department_id)
ALTER TABLE employees ADD CONSTRAINT FK_employees_manager_id FOREIGN KEY (manager_id) REFERENCES employees (employee_id)
ALTER TABLE HOTEL ADD CONSTRAINT FK_HOTEL_CITY_ID FOREIGN KEY (CITY_ID) REFERENCES CITY (ID)
ALTER TABLE REVIEW ADD CONSTRAINT FK_REVIEW_HOTEL_ID FOREIGN KEY (HOTEL_ID) REFERENCES HOTEL (ID)
ALTER TABLE departments ADD CONSTRAINT FK_departments_manager_id FOREIGN KEY (manager_id) REFERENCES employees (employee_id)
ALTER TABLE departments_employees ADD CONSTRAINT FK_departments_employees_employees_employee_id FOREIGN KEY (employees_employee_id) REFERENCES employees (employee_id)
ALTER TABLE departments_employees ADD CONSTRAINT FK_departments_employees_Department_department_id FOREIGN KEY (Department_department_id) REFERENCES departments (department_id)
CREATE SEQUENCE departments_seq START WITH 1
CREATE SEQUENCE employees_seq INCREMENT BY 10 START WITH 10
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
